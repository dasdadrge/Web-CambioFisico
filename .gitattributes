# Normalise line endings so a checkout on another OS doesn't show every file
# as modified. Windows keeps CRLF in the working copy; the repo stores LF.
* text=auto eol=lf

# dev.cmd only parses correctly with CRLF — see the comment inside it.
*.cmd text eol=crlf

*.png binary
*.jpg binary
*.ico binary
